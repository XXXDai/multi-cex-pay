# ---------------------------------------------------------------------------
# 运行时机密与业务数据，绝对不能提交
# ---------------------------------------------------------------------------
# 默认数据目录（CEXPAY_DATA_DIR），里面有数据库、凭据文件、收款码原图
data/
# 交易所 API Key/Secret 落盘文件（0600，可能是 Fernet 加密后的密文）
credentials.json
# 环境变量文件，通常直接写着 ADMIN_TOKEN / WEBHOOK_SECRET / MASTER_KEY
.env
.env.*
!.env.example
# 订单库（含订单金额、付款方昵称/UID 等个人信息）
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
*.db
# 日志（会记录订单号、金额、核销过程）
payment.log
*.log
# `cexpay qr compose` 的默认输出，含真实收款码
aggregate.png

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
env/
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# ---------------------------------------------------------------------------
# 测试与工具缓存
# ---------------------------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ---------------------------------------------------------------------------
# 编辑器 / 操作系统
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# 本地编辑器 / 工具配置
.claude/
.idea/
.vscode/
